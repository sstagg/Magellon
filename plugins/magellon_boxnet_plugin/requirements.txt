# Magellon Plugin SDK — shared rabbitmq / logging / bootstrap / events helpers.
# Bundled wheel; survives archive extraction.
./wheels/magellon_sdk-2.4.0-py3-none-any.whl[rabbitmq,nats,logging,config]

fastapi
uvicorn
pydantic
rich
pyyaml

python-json-logger

prometheus_client
prometheus-fastapi-instrumentator
python-dotenv

pika==1.3.2

mrcfile>=1.5.0
numpy>=1.26.4
scipy>=1.13.0
scikit-image>=0.22.0
Pillow>=10.4.0

# CPU torch is enough for the default deployment — pinned to PyTorch's
# CPU-only wheel index so the image doesn't drag in ~3 GB of CUDA libs
# (default PyPI Linux torch is the CUDA-bundled build). For a CUDA
# deployment, swap the index to https://download.pytorch.org/whl/cu121.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# BoxNet's .pt is an onnx2torch graph; module-load needs this even
# though plugin code never imports onnx2torch directly.
onnx2torch>=1.5
