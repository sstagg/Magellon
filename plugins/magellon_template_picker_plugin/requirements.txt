# Magellon Plugin SDK — shared rabbitmq / logging / bootstrap / events helpers.
# Bundled wheel; survives archive extraction.
./wheels/magellon_sdk-2.2.0-py3-none-any.whl[rabbitmq,nats,logging,config]

fastapi
uvicorn
pydantic
rich
pyyaml

python-json-logger

prometheus_client
python-dotenv

pika==1.3.2

mrcfile>=1.5.0
numpy>=1.26.4
scipy>=1.13.0
Pillow>=10.4.0

# Used by plugin/compute.py for the preview-cache TTLCache + LRUCache.
cachetools>=5.3
