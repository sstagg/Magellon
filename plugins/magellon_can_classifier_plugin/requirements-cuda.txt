# CUDA variant of requirements.txt — used by Dockerfile.cuda for GPU
# production deployments. Identical to the CPU requirements except
# torch is pulled from the cu121 wheel index (CUDA 12.1 runtime) so
# the algorithm runs on-device. Keep the two files in lockstep when
# bumping any pin.
#
# Magellon Plugin SDK — shared rabbitmq / logging / bootstrap / events helpers.
./wheels/magellon_sdk-2.5.0-py3-none-any.whl[rabbitmq,nats,logging,config]

fastapi
uvicorn
pydantic
rich
pyyaml

python-json-logger

prometheus_client
python-dotenv

pika==1.3.2

mrcfile==1.5.0
numpy==1.26.4
scipy==1.12.0

# Phase 7b runtime deps — torch from the CUDA 12.1 wheel index so the
# vendored CAN algorithm runs on-device. The base image
# (nvidia/cuda:12.1.1-runtime) supplies the matching CUDA runtime libs.
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.0,<3.0
scikit-image>=0.22,<0.25
scikit-learn>=1.4,<2.0
