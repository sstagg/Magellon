# Magellon Plugin SDK — shared rabbitmq / logging / bootstrap / events helpers.
# Bundled wheel; survives archive extraction.
./wheels/magellon_sdk-2.5.0-py3-none-any.whl[rabbitmq,nats,logging,config]

fastapi
uvicorn
pydantic
rich
pyyaml

python-json-logger

prometheus_client
python-dotenv

pika==1.3.2

mrcfile==1.5.0
numpy==1.26.4
scipy==1.12.0

# Phase 7b runtime deps (2026-05-03 vendor). Required to run the
# vendored algorithm in plugin/algorithm/classifier.py end-to-end.
# Plugin contract tests don't need them — compute.py loads them lazily
# via ``from plugin.algorithm import ...`` inside classify_stack.
#
# torch: pinned to the CPU-only build via the dedicated PyTorch index.
# The default PyPI Linux wheel bundles CUDA libs (~3 GB); the cpu
# wheel is ~200 MB. For GPU deployments use the CUDA Dockerfile and
# swap the index to https://download.pytorch.org/whl/cu121.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0,<3.0
scikit-image>=0.22,<0.25
scikit-learn>=1.4,<2.0
